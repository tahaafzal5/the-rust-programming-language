// Library named restaurant

// the contents of lib.rs form a module named crate at  the root of the crate's
// module structure known as the module tree.

mod back_of_house;
mod customer;
mod front_of_house;

fn deliver_order() {}
